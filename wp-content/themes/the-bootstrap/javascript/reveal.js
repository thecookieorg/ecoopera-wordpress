// When the DOM is ready, initialize the scripts.
// This is for Building Commissioning box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerBuilding" );
 

// Bind the link to toggle the slide.
$( ".revealBuilding" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});

// This is for Industrial Commissioning box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerIndustrial" );
 

// Bind the link to toggle the slide.
$( ".revealIndustrial" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Measurement and Verification box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerMeasurement" );
 

// Bind the link to toggle the slide.
$( ".revealMeasurement" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Energy Performance box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerEnergyPerf" );
 

// Bind the link to toggle the slide.
$( ".revealEnergyPerf" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Continuous Optimization box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerContinuous" );
 

// Bind the link to toggle the slide.
$( ".revealContinuous" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Energy Optimization box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerEnergyMng" );
 

// Bind the link to toggle the slide.
$( ".revealEnergyMng" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Retrofit Design box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerRetrofit" );
 

// Bind the link to toggle the slide.
$( ".revealRetrofit" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});

// This is for Sustainability Management box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerSust" );
 

// Bind the link to toggle the slide.
$( ".revealSust" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

});



// THIS IS FOR ABOUT US PAGE - REVEAL FUNCTIONS

// When the DOM is ready, initialize the scripts.
// This is for Management link on About Us - People page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerManagement" );
 

// Bind the link to toggle the slide.
$( ".revealManagement" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});

// When the DOM is ready, initialize the scripts.
// This is for Associates link on About Us - People page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerAss" );
 

// Bind the link to toggle the slide.
$( ".revealAss" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});

// When the DOM is ready, initialize the scripts.
// This is for Engineering Team link on About Us - People page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerEng" );
 

// Bind the link to toggle the slide.
$( ".revealEng" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});


// When the DOM is ready, initialize the scripts.
// This is for Building Commissioning box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerEco" );
 

// Bind the link to toggle the slide.
$( ".revealEco" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});



// When the DOM is ready, initialize the scripts.
// This is for Building Commissioning box on the home page.
jQuery(function( $ ){
 
// Get a reference to the container.
var container = $( ".containerProducts" );
 

// Bind the link to toggle the slide.
$( ".revealProducts" ).click(
function( event ){
// Prevent the default event.
event.preventDefault();
 
// Toggle the slide based on its current
// visibility.
if (container.is( ":visible" )){
 
// Hide - slide up.
container.slideUp( 1000 );
 
} else {
 
// Show - slide down.
container.slideDown( 1000 );
 
}
}
);

 
});